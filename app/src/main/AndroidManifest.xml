<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:versionCode="8"
    android:versionName="1.0">

    <uses-permission android:name="com.android.vending.CHECK_LICENSE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <attribution
        android:tag="playback"
        android:label="@string/attribution_playback_label" />
    <!-- Empty tag: system may request attribution with empty string (e.g. legacy code paths). -->
    <attribution
        android:tag="@string/attribution_empty_tag"
        android:label="@string/attribution_playback_label" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|screenSize"
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".PlaybackService"
            android:foregroundServiceType="mediaPlayback"
            android:attributionTags="playback"
            android:exported="false" />
        <receiver
            android:name=".CountdownAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.github.senz.binaural.COUNTDOWN_END" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
